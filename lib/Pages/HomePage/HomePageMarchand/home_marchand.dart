import 'package:flutter/material.dart';

class HomeMarchand extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Marchand')),
      body: Center(child: Text('Bienvenue Marchand!')),
    );
  }
}
