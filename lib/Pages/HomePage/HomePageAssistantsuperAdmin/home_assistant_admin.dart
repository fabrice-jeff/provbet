import 'package:flutter/material.dart';

class HomeAssistantAdmin extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Assistant Admin')),
      body: Center(child: Text('Bienvenue Assistant Admin!')),
    );
  }
}
